# v6.5 — composition-guide retries

- Project: `project-160d2a7c63ce`
- Parent code checkpoint: `strict-visual-target-v6.4`
- Database snapshot: `v6.5-before-composition-guide-retries.sqlite`
- SHA-256: `b4ad30367d4b4c0b6872d2189329baae20b3c94c8896b2b8215c6b12625707a2`
- Snapshot moment: strict gate evidence preserved; no images approved and no videos generated.

Paid retries now use a deterministic new attempt seed. Dependent image-edit calls use two references: the full continuity master for identity and set geometry, plus a vision-selected 16:9 crop of that master for the requested camera scale.

To restore, stop the local server and replace `data/videoforge.db` with the SQLite snapshot, then restart the server on the matching Git tag.
