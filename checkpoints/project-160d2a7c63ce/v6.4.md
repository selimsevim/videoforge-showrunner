# v6.4 — strict visual-target framing gate

- Project: `project-160d2a7c63ce`
- Parent project checkpoint: `practical-pillow-reveal-v6.3`
- Database snapshot: `v6.4-before-strict-framing-retries.sqlite`
- SHA-256: `5e60696d214570ba002822eb359e8e14bf3808b3a02126505bd4b683e8e9c0c8`
- Snapshot moment: failed v6.3 dependent-frame evidence preserved; no images approved and no videos generated.

The framing gate now checks the named visual target rather than the actor body ledger, validates medium shots, enforces physical Polaroid scale, forbids unsafe fallback crops, and rechecks every applied crop.

To restore, stop the local server and replace `data/videoforge.db` with the SQLite snapshot, then restart the server on the matching Git tag.
