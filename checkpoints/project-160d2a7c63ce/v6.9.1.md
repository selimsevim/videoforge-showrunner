# v6.9.1 — before strict shot-06 insert

- Project: `project-160d2a7c63ce`
- Parent code checkpoint: `dynamic-ots-clean-border-v6.9`
- Rollback database snapshot: `v6.9.1-before-shot6-insert.sqlite`
- SHA-256: `2b3d9edc3c36993a803ac130287bd156fa2e9021f283e5179f55d7fd1013f80b`
- Snapshot integrity: `ok`
- Snapshot moment: shot-05 retry 4 produced a strong single-subject OTS but was correctly rejected for `06.` on the front border; shot-06 retry 2 passed automation but was manually rejected because the Polaroid was oversized and Elena's torso remained visible. No images are approved and no videos have been generated.

The next experiment changes only shot-06 coverage from a generic close-up to a true physical-photo detail/insert. The first-frame target becomes the face-up Polaroid plus only the declared holding fingertips; face, head, neck, torso, and room geography must remain out of frame.

To roll back, stop the local server, select the parent Git tag, and replace `data/videoforge.db` with this SQLite snapshot.
