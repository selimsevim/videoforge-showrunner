# v6.6 — verified local reframe

- Project: `project-160d2a7c63ce`
- Parent code checkpoint: `composition-guide-retries-v6.5`
- Database snapshot: `v6.6-before-local-reframe.sqlite`
- SHA-256: `8b2d712d939f8e02860caca5f20f16d7b06abd299b537487f8ab4569a41d14ce`
- Snapshot moment: improved shot-03 Qwen source preserved as a failed framing attempt; no images approved and no videos generated.

A failed Qwen image may now be reframed without another image-generation call. Target-box fallback is limited to medium, close, and detail coverage, operates on a working copy, and is promoted only after a second vision inspection passes. Over-the-shoulder framing never uses this fallback.

To restore, stop the local server and replace `data/videoforge.db` with the SQLite snapshot, then restart the server on the matching Git tag.
